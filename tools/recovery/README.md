# Test-audio recovery tools

- Restore private audio fixtures from a local library.
- Audio is never downloaded or distributed.
- Pass `--search-root /path/to/samples` for external libraries.
- Review `--help` before copying files.
