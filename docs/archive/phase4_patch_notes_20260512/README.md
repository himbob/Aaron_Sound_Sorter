# Phase 4 Patch Notes Archive

These files are older working-session notes and handoff/status documents that used to live in the project root.

They are kept for history, but the active project entry points are:

- `README.md`
- `MASTER_PROJECT_GOALS.md`
- `Aaron_Sound_Sorter.py`
- `src/aaron_sound_sorter/`
- `commands/`
- `tests/`

The two-voter redesign status note is archived here too because the implementation now lives in code:

- `src/aaron_sound_sorter/two_voter.py`
- `tests/test_two_voter_redesign.py`
